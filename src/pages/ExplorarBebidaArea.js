import React from 'react';
import Header from '../components/Header';

function ExplorarBebidaArea() {
  const explorar = 'Explorar';
  return (
    <Header title={ explorar } search={ false } />
  );
}

export default ExplorarBebidaArea;
